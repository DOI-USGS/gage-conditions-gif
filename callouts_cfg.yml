-
  event_dates:
    start: "2022-04-01"
    end: "2022-05-02"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-04-01"
    end: "2022-05-02"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: Drought persists in much of the West
    # This is in percentage space, relative to the plotting device
    x_loc: 0.50
    y_loc: 0.75
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 4
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#ca0020"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-04-05"
    end: "2022-04-21"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-04-05"
    end: "2022-04-21"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["Fronts bring high water", "to parts of the Eastern", "and Southern U.S."]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.75
    y_loc: 0.60
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 2
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#04507d"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-04-23"
    end: "2022-06-07"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-04-23"
    end: "2022-06-07"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["Rain and snowmelt", "bring flooding to", "the Upper Midwest"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.40
    y_loc: 0.80
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 4
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#04507d"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-04-27"
    end: "2022-05-06"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-04-27"
    end: "2022-05-06"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["A dry period", 'for the', "Mid-Atlantic"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.90
    y_loc: 0.68
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 4
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#ca0020"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-05-03"
    end: "2022-06-30"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-05-03"
    end: "2022-06-30"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: Drought deepens in the Southwest
    # This is in percentage space, relative to the plotting device
    x_loc: 0.60
    y_loc: 0.60
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 4
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#ca0020"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-05-04"
    end: "2022-05-11"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-05-04"
    end: "2022-05-11"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["High water", "in the Midwest"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.75
    y_loc: 0.40
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 3
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#04507d"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-05-09"
    end: "2022-06-08"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-05-09"
    end: "2022-06-08"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["A dry period", "for New England"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.80
    y_loc: 0.85
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 2
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#ca0020"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-06-05"
    end: "2022-06-22"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-06-05"
    end: "2022-06-22"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["Atmospheric river brings", "high water to the", "Pacific Northwest"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.38
    y_loc: 0.85
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 4
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#04507d"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
-
  event_dates:
    start: "2022-06-09"
    end: "2022-06-25"
  text_dates:
    # Adjust these if the text + callout shape should appear for
    #   different dates than the event appears on the date wheel
    start: "2022-06-09"
    end: "2022-06-25"
  # Defaults to 9
  fade_in:
  # Defaults to 9
  fade_out:
  text:
    label: ["Atmospheric river leads", "to record flooding", "on the Yellowstone River"]
    # This is in percentage space, relative to the plotting device
    x_loc: 0.45
    y_loc: 0.75
    # Numeric indicating where to place text relative to location: 1,2,3,4 (below, left, above, right)
    pos: 4
    cex: 6
  # TRUE or FALSE, defaults to FALSE
  add_box:
  # "#04507d" for wet event & "#ca0020" for a dry event, defaults to wet
  wheel_color: "#04507d"
  # Integer, starting with 0 (use >= 1 to increase height)
  wheel_hierarchy: 1
  polygon:
    # Defaults to existing small NE shape
    file: "6_visualize/in/overlay_callouts_dummy.png"
